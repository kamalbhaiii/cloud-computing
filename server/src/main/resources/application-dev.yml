
logging:
  level:
    edu:
      fra:
        uas: DEBUG
spring:
  application:
    name: Backend
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/cloud-ai
    username: root
    password: cloud-ai
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
server:
  address: 127.0.0.1
  port: 9090
