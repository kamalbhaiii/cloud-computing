This project is built using a distributed hardware architecture that includes edge computing nodes (Raspberry Pi-based sensor units), a centralized Kubernetes cluster (for backend/frontend deployment and storage), and supporting peripherals. The hardware is designed to be portable, scalable, and suitable for field deployment in outdoor environments.

🧱 1. Hardware Inventory:

The project kit includes the following hardware components:
<table> <thead> <tr> <th>Quantity</th> <th>Item Description</th> </tr> </thead> <tbody> <tr> <td>1x</td> <td>Raspberry Pi 5 (8GB RAM) – Sensor Node</td> </tr> <tr> <td>1x</td> <td>Raspberry Pi 4 – Optional secondary sensor node or edge processing</td> </tr> <tr> <td>6x</td> <td>Raspberry Pi 3 – Kubernetes Cluster nodes</td> </tr> <tr> <td>2x</td> <td>Raspberry Pi Camera Module v2 (8MP)</td> </tr> <tr> <td>1x</td> <td>Google Coral USB Accelerator (for ML inference acceleration)</td> </tr> <tr> <td>1x</td> <td>Gigabit Ethernet Switch</td> </tr> <tr> <td>9x</td> <td>RJ45 Ethernet Cables</td> </tr> <tr> <td>10x</td> <td>MicroSD Cards (32GB each)</td> </tr> <tr> <td>1x</td> <td>USB 3.0 SD/MicroSD Card Reader</td> </tr> <tr> <td>3x</td> <td>SD Card Adapters</td> </tr> <tr> <td>1x</td> <td>Raspberry Pi 27W USB-C Power Supply</td> </tr> <tr> <td>1x</td> <td>Raspberry Pi 15W USB-C Power Supply</td> </tr> <tr> <td>1x</td> <td>Anker 60W USB Power Supply Hub</td> </tr> <tr> <td>6x</td> <td>Micro USB Power Cables</td> </tr> <tr> <td>60x</td> <td>M2.5 Spacer Bolts</td> </tr> <tr> <td>1x</td> <td>Aluminum Carrying Case (460x340x160 mm)</td> </tr> </tbody> </table>

🖼️ 2. System Layout:

The hardware system is split into two major components:

A. Sensor Node(s):

1. Raspberry Pi4(Primary)
2. Camera Module connected 
3. Google Coral USB Accelerator connected via USB 3.0
4. Runs object detection locally using YOLO/Roboflow
5. Sends detection events (REST) to backend

B. Kubernetes Cluster:

6x Raspberry Pi 3 Boards form a k3s-based HA Kubernetes Cluster.

Deployed components:

1. Backend API services (Springboot)
2. Frontend (Reactjs)
3. Object storage (MinIO with S3 API)

Power and network managed via Anker power hub and Ethernet switch.

🔗 3. Connectivity & Networking:

1. All devices are interconnected via the Gigabit Ethernet switch.
2. Static IP configuration or DHCP with known mapping.
3. Sensor nodes and cluster nodes communicate over LAN using REST.


