**RPi initial Setup steps:**

Step-1: Flash the SD card with Ubuntu Server LTS or Raspberry Pi OS (Bookworm)

Step-2: Set the username: and password:

Step-3: Login into Raspberry Machines using: ssh <username>@<raspberry-pi-ip>

Step-4: After login, run in terminal sudo apt update && sudo apt upgrade -y

Step-5: Run in terminal, sudo nano /boot/firmware/cmdline.txt

Step-6: Add at the end, cgroup_enable=1 cgroup_memory=1, save the file.

Step-7: Run in the terminal, sudo reboot

Step-8: Continue the k3s configuration.



**Installation of k3s:**

1) Master Node:

step 1:
`curl -sfL https://get.k3s.io | sh -` 

step 2:
`sudo cat /var/lib/rancher/k3s/server/node-token` [Copy the output of this command]

step 3:
`sudo kubectl label node <master-node-name> node-role.kubernetes.io/worker=master `

2) Worker Nodes:

step 1:
`curl -sfL https://get.k3s.io | K3S_URL=https://<master-node-ip>:6443 K3S_TOKEN=<node-token> sh -`

step 2:
`sudo kubectl label node <worker-node-name> node-role.kubernetes.io/worker=worker`



**Node configuration:**
## Node Configuration Table

| Parameter              | Description                                                                 |
|------------------------|-----------------------------------------------------------------------------|
| Node Name / ID         | master-node and worker-node-<1-5>                                           |
| IP Address / Hostname  | 192.168.137.0                                                               |
| Role                   | master, worker                                                              |
| CPU / Memory           | Master-Node: 8 GB RAM, 32GB Disk Memory and 4-cores Processor               |
|                        |  Worker-Node: 1 GB RAM, 32 GB Disk Memory and 4-cores Processor             |
|                        |  Sensor-Node: 4 GB RAM, 32 GB Disk Memory and 4-cores Processor             |
| Services / Applications| Master-Node: Jenkins, Prometheus and Grafana                                |
|                        | Worker-Node(s): Frontend, Backend, PostgreSQL Database and MinIO Bucket     |
|                        | Sensor-Node: Roboflow Model                                                 |
| Storage Configuration  | Using Persistent Volume and Persistent Volume Claim                         |
| Authentication         | NodePort Service                                                            |
| Monitoring             | Prometheus and Grafana                                                      |


Open-Access URL(s):

http://192.168.137.178:8080/ -> Jenkins

http://frontend.local/ -> Frontend Dashboard

http://server.local/ -> Server

http://grafana.local/ -> Monitoring Dashboard

| IP Address       | Node Type       |
|------------------|-----------------|
| 192.168.137.178  | Master Node     |
| 192.168.137.179  | Worker-Node-1   |
| 192.168.137.180  | Worker-Node-2   |
| 192.168.137.181  | Worker-Node-3   |
| 192.168.137.182  | Worker-Node-4   |
| 192.168.137.183  | Worker-Node-5   |
| 192.168.137.184  | Sensor-Node     |



## Low Level Architecture
<img src="https://github.com/user-attachments/assets/12380e1f-86b2-4660-b575-02b8aacfcbe6" width="50%" />



**Helm or manual YAML deployments:**
Helm is required only on master-node for Prometheus and Grafana Installation:

To install Helm-charts:

`$ curl -fsSL -o get_helm.sh https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3`

`$ chmod 700 get_helm.sh`


`$ /get_helm.sh`

To install Prometheus and Grafana:

`$ helm repo add prometheus-community https://prometheus-community.github.io/helm-charts`

Apply config files of the Prometheus (From Repo):

`$ helm upgrade prometheus prometheus-community/prometheus -f ./config/prometheus-values.yaml`



